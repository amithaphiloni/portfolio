# Public assets

Drop the following files into this `public/` folder so they're served at the site root:

- `resume.pdf` — your resume (linked by the Navbar "Resume" button and the Experience section's "Download Full Resume" CTA).
- `favicon.svg` — optional favicon referenced from `index.html`.
