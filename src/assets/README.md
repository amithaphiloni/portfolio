# Assets

Place your profile picture here as **`profile.jpg`** (or update the import in
`src/components/Hero.jsx`).

The Hero component imports it like so:

```jsx
import profilePic from '../assets/profile.jpg'
```

Recommended: a square crop (e.g. 800×800) — the Hero renders it inside a
circular frame with an animated gradient ring.
